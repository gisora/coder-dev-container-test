#!/usr/bin/env bash

echo "on-create"

# set -euo pipefail

# cd "${containerWorkspaceFolder:-/workspaces/coder-dev-container-test}"

# # Create the virtual environment (uses Python 3.14 from the base image)
# # --clear replaces any existing .venv left over from a previous workspace start
# uv venv --clear